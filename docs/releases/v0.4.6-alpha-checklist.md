# v0.4.6-alpha Checklist

Use this checklist for the MCP performance benchmark integration gate.

- [ ] Public PR #100 is merged and `v0.4.5-alpha` is tagged.
- [ ] Public PR #99 is merged into `main`.
- [ ] `pyproject.toml`, `unlimited_skills/__init__.py`, Claude plugin metadata,
      and marketplace metadata all report `0.4.6`.
- [ ] `scripts/run-v046-alpha-mcp-performance-smoke.py --fixture-mode` passes.
- [ ] `scripts/verify-v046-alpha-mcp-performance.py --expected-sha <sha>`
      passes.
- [ ] `tests/integration/test_v046_alpha_mcp_performance.py` passes.
- [ ] `tests/test_mcp_performance_benchmarks.py` passes.
- [ ] Full pytest passes before merge.
- [ ] Compileall and `git diff --check` pass.
- [ ] No private keys, tokens, prompts, local paths, skill bodies, or private
      pack bodies are introduced into public docs or reports.
- [ ] Codex does not create or push `v0.4.6-alpha` from this integration gate.

Release owner follow-up:

- [ ] Create a separate final publication gate before tagging `v0.4.6-alpha`.
