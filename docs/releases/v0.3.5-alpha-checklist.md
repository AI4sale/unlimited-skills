# v0.3.5-alpha Checklist

- [ ] Private registry community review workflow PR accepted.
- [ ] Public community catalog client v2 PR accepted.
- [ ] Cross-repo community catalog E2E passes in fixture mode.
- [ ] Public full tests pass.
- [ ] Private registry operator gates pass.
- [ ] No production hosted calls in release tests.
- [ ] No skill bodies, private paths, secrets, payment fields, or private signing keys committed.
- [ ] Release owner approves merge/tag separately.

Required commands:

```powershell
python scripts/run-community-catalog-cross-repo-e2e.py --fixture-mode --temp-home --json
python scripts/run-v0.3.5-alpha-community-catalog-smoke.py --fixture-mode --temp-home
python scripts/run-v0.3.5-alpha-release-smoke.py
python scripts/verify-v0.3.5-alpha-community-catalog.py --expected-sha <tag-target-sha>
python scripts/verify-v0.3.5-alpha-publication.py --expected-sha <tag-target-sha>
python scripts/run-v0.2x-smoke-tests.py
python -m pytest tests -q
python -m compileall -q unlimited_skills scripts tests
git diff --check
```

Human tag command after release-owner approval:

```bash
python scripts/verify-v0.3.5-alpha-publication.py --expected-sha <tag-target-sha>
git tag -a v0.3.5-alpha <tag-target-sha> -m "v0.3.5-alpha"
git push origin v0.3.5-alpha
```
