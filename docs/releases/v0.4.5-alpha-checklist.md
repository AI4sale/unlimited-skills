# v0.4.5-alpha Checklist

Use this checklist for the MCP audit inspector integration gate.

- [ ] Public PR #95 is merged into `main`.
- [ ] Public PR #97 is merged and `v0.4.4-alpha` remains the latest published
      tag before this integration gate.
- [ ] `pyproject.toml`, `unlimited_skills/__init__.py`, Claude plugin metadata,
      and marketplace metadata all report `0.4.5`.
- [ ] `scripts/run-v045-alpha-mcp-audit-inspector-smoke.py --fixture-mode`
      passes.
- [ ] `scripts/verify-v045-alpha-mcp-audit-inspector.py --expected-sha <sha>`
      passes.
- [ ] `tests/integration/test_v045_alpha_mcp_audit_inspector.py` passes.
- [ ] `tests/test_mcp_audit_inspector.py` passes.
- [ ] `scripts/run-v045-alpha-release-smoke.py` passes.
- [ ] `scripts/verify-v045-alpha-publication.py --expected-sha <sha>`
      passes.
- [ ] Full pytest passes before merge.
- [ ] Compileall and `git diff --check` pass.
- [ ] No private keys, tokens, prompts, local paths, skill bodies, or private
      pack bodies are introduced into public docs.
- [ ] Codex creates and pushes `v0.4.5-alpha` only after the final publication
      verifier passes on the selected `main` SHA.

Release owner follow-up:

- [ ] Confirm the pushed `v0.4.5-alpha` tag target matches the verified
      selected `main` SHA.
