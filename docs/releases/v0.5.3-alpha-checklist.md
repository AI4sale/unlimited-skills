# v0.5.3-alpha Checklist

- [ ] Package/plugin/marketplace versions are `0.5.3`.
- [ ] `pip install unlimited-skills==0.5.3` is the final post-publish smoke
  package.
- [ ] `unlimited-skills --version` prints `unlimited-skills 0.5.3`.
- [ ] A4.9 local event privacy audit from PR #150 is included.
- [ ] A4.10 local event privacy hardening from PR #151 is included.
- [ ] Local search/list/view/use event rows do not persist raw task/query/filter
  text or absolute skill paths.
- [ ] Local feedback rows do not persist raw query text or freeform notes.
- [ ] Local search hits persist `score_bucket` rather than exact scores.
- [ ] Local task/query values persist as `query_summary_hash` or
  `task_summary_hash`, not raw text.
- [ ] Local notes persist as `notes_present` and `notes_length_bucket`, not raw
  text.
- [ ] Public stdout/API behavior remains unchanged.
- [ ] `learning-summary --events` returns aggregate local metrics only.
- [ ] `feedback prepare` remains redacted and paste-safe.
- [ ] `scripts/run-v053-alpha-package-smoke.py --json` passes.
- [ ] `scripts/verify-v053-alpha-publication.py --package-availability prepublish`
  passes on the selected release commit.
- [ ] `scripts/verify-v053-alpha-publication.py --package-availability published`
  passes only after real PyPI upload and clean install smoke.
- [ ] `.github/workflows/publish-pypi.yml` requires exact `0.5.3` inputs and
  manual Trusted Publishing confirmation.
- [ ] #119 remains explicitly excluded/background.
- [ ] No marketplace submission happens in this release PR.
- [ ] No paid CTA, payment link, hosted readiness claim, Team readiness claim,
  Enterprise readiness claim, support/SLA promise, or delivery promise is
  added.
- [ ] No telemetry, tracking pixel, analytics SDK, automatic upload, hosted
  query forwarding, private social scraping, prompt collection, tool input
  collection, or tool output collection is added.
- [ ] Tag `v0.5.3-alpha` is created only after final verifier approval.
