# v0.5.2-alpha Checklist

- [ ] Package/plugin/marketplace versions are `0.5.2`.
- [ ] `pip install unlimited-skills==0.5.2` is the final post-publish smoke
  package.
- [ ] `unlimited-skills --version` prints `unlimited-skills 0.5.2`.
- [ ] A4.7 public-alpha signal rollup generator is included.
- [ ] A3.1.1 effectiveness v2 instrumentation is included.
- [ ] `learning-summary --events` returns aggregate local metrics only.
- [ ] Default `learning-summary` output remains unchanged without `--events`.
- [ ] Public `suggest --json` stdout contract remains unchanged.
- [ ] Raw session ids are never persisted.
- [ ] Aggregate metrics contain no query text, prompts, skill bodies,
  filesystem paths, environment values, tokens, or tool outputs.
- [ ] `scripts/run-v052-alpha-package-smoke.py --json` passes.
- [ ] `scripts/verify-v052-alpha-publication.py --package-availability prepublish`
  passes on the selected release commit.
- [ ] `scripts/verify-v052-alpha-publication.py --package-availability published`
  passes only after real PyPI upload and clean install smoke.
- [ ] `.github/workflows/publish-pypi.yml` requires exact `0.5.2` inputs and
  manual Trusted Publishing confirmation.
- [ ] #119 remains explicitly excluded/background.
- [ ] No marketplace submission happens in this release PR.
- [ ] No paid CTA, payment link, hosted readiness claim, Team readiness claim,
  Enterprise readiness claim, support/SLA promise, or delivery promise is
  added.
- [ ] No telemetry, tracking pixel, analytics SDK, automatic upload, hosted
  query forwarding, private social scraping, prompt collection, tool input
  collection, or tool output collection is added.
- [ ] Tag `v0.5.2-alpha` is created only after final verifier approval.
