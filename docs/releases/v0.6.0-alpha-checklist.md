# v0.6.1-alpha Checklist

- [ ] Package/plugin/marketplace versions are `0.6.1`.
- [ ] `pip install unlimited-skills==0.6.1` is the final post-publish smoke
  package.
- [ ] `unlimited-skills --version` prints `unlimited-skills 0.6.1`.
- [ ] v0.6 contract freeze spec from PR #156 is included.
- [ ] v0.6 local ROI receipt spec from PR #157 is included.
- [ ] v0.6 local ROI receipt runtime from PR #158 is included.
- [ ] `quickstart --json` works from a clean installed wheel.
- [ ] `suggest ... --json` works from a clean installed wheel.
- [ ] `mcp savings --json` works from a clean installed wheel.
- [ ] `mcp install --claude-code --dry-run --json` works from a clean installed
  wheel.
- [ ] `feedback prepare --format json` remains redacted and paste-safe.
- [ ] `learning-summary --events --json` returns aggregate local metrics only.
- [ ] `roi receipt` emits screenshot-friendly Markdown.
- [ ] `roi receipt --format json` emits schema-versioned `local_roi_receipt`
  JSON.
- [ ] `roi receipt --out <file>` writes locally without printing the local
  output path.
- [ ] `roi receipt --since 7d` filters the safe aggregate window where possible.
- [ ] ROI receipt output contains the required local-only notice.
- [ ] ROI receipt output does not include prompts, raw queries, raw tasks, tool
  inputs, tool outputs, skill bodies, MCP schemas, raw logs, raw configs, env
  names or values, tokens, keys, local absolute paths, user identifiers, or
  tracking identifiers.
- [ ] `scripts/verify-v06-frozen-contracts.py --json` passes before any PyPI
  publish or tag guidance.
- [ ] `scripts/run-v060-alpha-package-smoke.py --json` passes.
- [ ] `scripts/verify-v060-alpha-publication.py --package-availability
  prepublish` passes on the selected release commit and includes frozen
  contract pass evidence.
- [ ] `scripts/verify-v060-alpha-publication.py --package-availability
  published` passes only after real PyPI upload, clean install smoke, and
  frozen contract pass evidence.
- [ ] `.github/workflows/publish-pypi.yml` requires exact `0.6.1` inputs and
  manual Trusted Publishing confirmation.
- [ ] #119 remains explicitly excluded/background.
- [ ] No marketplace submission happens in this release PR.
- [ ] No paid CTA, payment link, hosted readiness claim, Team readiness claim,
  Enterprise readiness claim, support/SLA promise, delivery promise, ROI
  guarantee, or universal savings promise is added.
- [ ] No telemetry, tracking pixel, analytics SDK, automatic upload, hosted
  query forwarding, private social scraping, prompt collection, tool input
  collection, or tool output collection is added.
- [ ] Tag `v0.6.1-alpha` is created only after final verifier approval.
