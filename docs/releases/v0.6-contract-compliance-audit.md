# v0.6 Contract Compliance Audit

Status: passed with no runtime blocker for `v0.6.1-alpha`.

This audit checks the frozen v0.6 public-alpha contracts against actual
`unlimited-skills==0.6.1` behavior installed from PyPI. It is documentation
only: no runtime behavior, package release, marketplace submission, hosted
readiness, team readiness, enterprise readiness, sales flow, payment flow, or
#119/E19 work is added.

v0.6.1-alpha is the valid v0.6 alpha release.
The 0.6.0 package was uploaded to PyPI but was not tagged or released.
The owner decision packet for the uploaded-but-not-released artifact lives in
[v0.6.0 uploaded-not-released incident](v0.6.0-uploaded-not-released-incident.md).

## Release Evidence

| Evidence | Result |
| --- | --- |
| PyPI package | `unlimited-skills==0.6.1` installed cleanly in a fresh virtual environment |
| Version command | `unlimited-skills --version` returned `unlimited-skills 0.6.1` |
| GitHub release | `v0.6.1-alpha` prerelease exists |
| Release URL | <https://github.com/AI4sale/unlimited-skills/releases/tag/v0.6.1-alpha> |
| Tag target | `3b30b41f751451331de231c352eff2bce3d4fddc` |
| Published verifier | `verify-v060-alpha-publication.py --package-availability published` passed |
| Open public PRs | only #119, parked E19 |
| v0.6.0 status | uploaded to PyPI, failed published verifier, not tagged, not the valid alpha |
| Valid v0.6 alpha | `v0.6.1-alpha` |
| v0.6.0 decision packet | [v0.6.0-uploaded-not-released-incident.md](v0.6.0-uploaded-not-released-incident.md) |

## Compliance Matrix

| Surface | Contract source | 0.6.1 evidence | Status | Drift or blocker |
| --- | --- | --- | --- | --- |
| `unlimited-skills --version` | CLI version surface | returned `unlimited-skills 0.6.1` from PyPI install | pass | none |
| `unlimited-skills quickstart --json` | `docs/cli-contracts.md` | returned valid JSON with `library`, redacted `root`, `savings`, `search`, `next_steps`, and local MCP savings summary | pass | none |
| `unlimited-skills suggest "Design a REST API for a service" --json` | `docs/cli-contracts.md` | returned `task_summary_hash`, top-3 candidates, `reason_code=match_found`, `recommended_next_action`, and latency without echoing raw task text | pass | none |
| `unlimited-skills mcp savings --json` | `docs/cli-contracts.md` | returned valid JSON with measured servers, byte/token estimates, gateway bytes, savings bytes, and savings percent | pass | none |
| `unlimited-skills mcp install --claude-code --dry-run` | `docs/cli-contracts.md` | produced a redacted dry-run plan for project `.mcp.json`, preserved gateway install scope, and did not require writes | pass | none |
| `unlimited-skills feedback prepare --json` | `docs/feedback.md`, `docs/cli-contracts.md` | returned schema-versioned local report with privacy flags, no hosted calls, no network calls, no raw configs, no prompts, no tool IO, no skill bodies, no tokens, and no local paths | pass | none |
| `unlimited-skills learning-summary --events --json` | `docs/cli-contracts.md` | returned aggregate `effectiveness` and `feedback` JSON; this is the fixed 0.6.1 surface that failed in the untagged 0.6.0 upload | pass | none |
| `unlimited-skills roi receipt` | `docs/roi-receipt.md` | returned Markdown receipt with version, library count, quickstart state, local MCP savings, routing aggregates, learning summary, feedback status, legacy log status, and required notice | pass | none |
| `unlimited-skills roi receipt --format json` | `docs/roi-receipt.md`, `schemas/roi-receipt.schema.json` | returned `report_type=local_roi_receipt`, `schema_version=1`, `unlimited_skills_version=0.6.1`, privacy flags, local-only notice, and no forbidden fields | pass | none |
| `unlimited-skills roi receipt --since 7d` | `docs/roi-receipt.md` | returned Markdown receipt for window `7d`, skipped unsafe legacy rows, and kept the required local-only/no-upload/no-telemetry notice | pass | none |
| `scripts/generate-public-alpha-signal-rollup.py` | `docs/adoption/public-alpha-signal-rollup-template.md` | `--help` succeeded and fixture mode generated a privacy-bounded rollup using public aggregate/local docs data only | pass | fixture values are deterministic examples, not release-current claims |
| Local event privacy after `v0.5.3-alpha` | `docs/local-event-privacy.md` | boundary verifier passed; audit evidence uses only aggregate `learning-summary`, `feedback prepare`, and ROI receipt output | pass | none |
| Feedback report schema | `docs/feedback.md` | `feedback prepare --json` returned `schema_version=1`, `report_type=feedback-prepare-report`, issue-template mapping, paste boundaries, privacy flags, and local-only status | pass | none |
| ROI receipt schema | `docs/roi-receipt.md` | published verifier and ROI boundary verifier passed; JSON output includes schema version, report type, privacy notice, and forbidden-field flags | pass | none |
| Stdout JSON stability | `docs/cli-contracts.md` | all checked `--json` surfaces returned parseable JSON with documented top-level fields and no forbidden private payloads | pass | none |
| Frozen contract regression harness | `scripts/verify-v06-frozen-contracts.py` | current-tree JSON harness checks version, quickstart, suggest, MCP install dry-run, MCP savings, feedback prepare, learning-summary events, ROI receipt Markdown/JSON/since, and signal-rollup fixture generation | pass | none |
| Release-gate integration for frozen contracts | `scripts/verify-v060-alpha-publication.py` | prepublish and published verifier modes now invoke `verify-v06-frozen-contracts.py --json`; drift blocks release/tag guidance with owner/action/fallback details | pass | none |
| PyPI Trusted Publishing and post-publish verifier | `docs/cli-contracts.md`, `docs/releases/v0.6.0-alpha-pypi-publishing.md` | workflow run `27485990875` succeeded; published-mode verifier passed before tag/prerelease creation; future verifier runs require frozen-contract pass evidence | pass | none |
| v0.6.0 uploaded-but-untagged incident | release gate | `0.6.0` failed published verifier because `learning-summary --events --json` was rejected; no `v0.6.0-alpha` tag or prerelease was created | pass | optional owner action: consider yanking or marking 0.6.0 as bad on PyPI |
| #119/E19 | release boundary | open PR list contains only #119; it remains parked and outside this release/audit | pass | none |

## Drift And Blockers

No runtime drift requiring a `v0.6.2` blocker was found.

| Item | Owner | Action | Fallback |
| --- | --- | --- | --- |
| PyPI `0.6.0` remains uploaded but untagged | release owner / PyPI project owner | Decide whether to yank `0.6.0`; if yes, collect PyPI yanked-state evidence with date, owner, and reason | Leave `0.6.0` untagged/unreleased and keep `0.6.1+` as the documented install target |
| Signal-rollup fixture still describes an older public-alpha snapshot | maintainer | Refresh fixture values only when the adoption rollup task explicitly asks for current release counters | Treat fixture values as deterministic examples, not release-current evidence |

## Evidence Commands

The audit used a fresh virtual environment with the PyPI package:

```bash
python -m pip install --no-cache-dir unlimited-skills==0.6.1
unlimited-skills --version
unlimited-skills quickstart --json
unlimited-skills suggest "Design a REST API for a service" --json
unlimited-skills mcp install --claude-code --dry-run
unlimited-skills mcp savings --json
unlimited-skills feedback prepare --json
unlimited-skills learning-summary --events --json
unlimited-skills roi receipt
unlimited-skills roi receipt --format json
unlimited-skills roi receipt --since 7d
```

Repository-side verification remains:

```bash
python -m pytest tests/test_public_alpha_docs.py -q
python scripts/verify-mcp-boundaries.py
python scripts/verify-feedback-report-boundaries.py
python scripts/verify-roi-receipt-boundaries.py
python scripts/verify-v06-frozen-contracts.py --json
python scripts/verify-v060-alpha-publication.py --package-availability prepublish --json
python -m pytest tests -q
git diff --check
```

## Boundaries Preserved

- No runtime behavior changes.
- No package release.
- No marketplace submission.
- No hosted, team, or enterprise readiness claim.
- No paid CTA, payment flow, support SLA, delivery promise, or
  ROI guarantee.
- No telemetry, upload, analytics SDK, tracking pixel, no hosted query forwarding,
  prompt collection, tool input collection, or tool output collection.
- #119/E19 remains parked unless the owner explicitly reopens that lane.
