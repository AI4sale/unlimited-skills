# Unlimited Skills v0.2.1-alpha

This public alpha finalizes the Local Skill Hub runtime stack and the signed hosted-manifest client boundary. It is a reproducible release after private registry PR #2 and public PR #13 through PR #19 are reviewed and merged.

## Highlights

- Local Skill Hub protected `/v1/...` APIs require a valid, non-revoked hub client token.
- `GET /health` remains unauthenticated for local liveness checks.
- Hub tokens can be created, listed, and revoked with `unlimited-skills hub token create|list|revoke`.
- Hub tokens are stored as SHA256 hashes; raw token values are shown only once at creation.
- LAN bind remains opt-in: non-localhost hosts require `--allow-lan` and at least one active hub token.
- Remote client runtime is implemented for `remote configure`, `remote status`, `remote search`, `remote resolve`, and `remote view`.
- Remote fallback policy is explicit: `local_allowed` can use the local library when the hub is unavailable; `hub_required` fails closed.
- Hub allowlist bootstrap can cache a validated local fixture allowlist or registered hosted allowlist metadata.
- The v0.2.x smoke suite now runs real hub token, remote client, and allowlist bootstrap scenarios on the integrated branch.
- Agent installers can render remote-first router instructions for Codex, Claude Code, Hermes, and OpenClaw.
- Runtime manifests and capability-aware dry-run install plans help route tool/platform skills without hub-side execution.
- Hosted manifest envelopes are required and Ed25519-verified for hub allowlist sync, collection updates, enhancement manifests, and team sync manifests.
- Bundled, local, and environment manifest trust keys support scope checks, registry-origin pinning, `trust import`, `trust revoke`, and `trust verify`.
- Hub client lifecycle, active-client quota, metrics, and redacted audit events are covered by the v0.2.x smoke suite.

## Product Boundary

- Existing `unlimited-skills serve` remains the free MIT local daemon and does not require registration.
- `unlimited-skills hub serve` remains registration-gated.
- Local search, list, view, where, reindex, vector-reindex, adapt, native sync, installers, migration scripts, and public self-update remain local-first and unregistered.
- Local Skill Hub remains alpha and allowlist-only.
- Full catalog distribution remains disabled.
- The launch policy follows the private registry audit verdict `YES_WITH_ALLOWLIST`.
- Hosted registry services do not receive local hub search queries by default.
- The hub does not execute skills.
- Remote-first installer mode writes local hub config and router instructions only. It does not registration-gate local MIT commands and does not store raw hub tokens in visible router files.
- Capability-aware install plans list required local packages, binaries, platforms, and environment variable names only. Secrets stay client-side.

## Security Notes

Current client-side hosted archive security still relies on SHA256 verification and safe extraction. Hosted manifest authenticity is checked first with required Ed25519 `manifest_signature`; archive bytes are accepted only after SHA256 verification.

For serious LAN deployment, run the hub behind reverse proxy or network controls that provide TLS, authentication, access logging, and IP allowlisting.

The public trusted manifest key id for this alpha is `registry-alpha-2026-06`. It is scoped to `hub-allowlist`, `catalog-updates`, `enhancement-manifest`, and `team-sync-manifest` for `https://unlimited.ai4.sale`. The private registry signing key is not shipped in the public repo.

## Verification

Required release checks:

```text
python -m pytest tests -q
python scripts/run-v0.2x-smoke-tests.py
python scripts/verify-v0.2.1-alpha-release.py
python scripts/run-v0.2.1-alpha-fresh-install-smoke.py
python scripts/run-v0.2.1-alpha-upgrade-smoke.py
python -m compileall -q unlimited_skills scripts tests
git diff --check
```

The smoke suite must use a temporary HOME, must not call production hosted registry services, and must not expose raw hub tokens, registration tokens, or device private keys in captured output.

The machine-readable release manifest is [v0.2.1-alpha.release-manifest.json](v0.2.1-alpha.release-manifest.json).

GitHub Actions must also be green on the final release-gate PR. See [v0.2.1-alpha-checklist.md](v0.2.1-alpha-checklist.md).

## Traceability

- Private registry PR #2: signed registry manifest publisher used by hosted registry artifacts.
- PR #13: v0.2.1-alpha Local Skill Hub runtime integration.
- PR #14: remote-first router installers.
- PR #15: capability-aware local install plans.
- PR #16: required signed hosted manifest verification.
- PR #17: v0.2.1-alpha CI and merge gate.
- PR #18: trusted key bootstrap, rotation, origin pinning, and local revocation.
- PR #19: Local Skill Hub client lifecycle, active-client quota, audit, and metrics.
- Finalization PR: release manifest, upgrade/fresh smoke, and tag checklist.

Tag `v0.2.1-alpha` only after the release owner approves the finalization PR evidence.
