{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/AI4sale/unlimited-skills/schemas/mcp-tool-index.schema.json",
  "title": "Unlimited Tools gateway indexed tool entry",
  "description": "One entry of the gateway's in-memory upstream tool index, as returned by tools_search hits. Input schemas are deliberately NOT part of the index: tools_search returns names and descriptions only, and tools_schema retrieves exactly one schema lazily.",
  "type": "object",
  "required": ["tool", "upstream", "name", "description"],
  "additionalProperties": false,
  "properties": {
    "tool": {
      "type": "string",
      "description": "Fully qualified id, '<upstream>.<tool>'."
    },
    "upstream": {
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9_-]*$"
    },
    "name": { "type": "string", "minLength": 1 },
    "description": { "type": "string" },
    "score": {
      "type": "number",
      "minimum": 0,
      "description": "Lexical relevance score for the current query."
    }
  },
  "not": { "required": ["inputSchema"] }
}
